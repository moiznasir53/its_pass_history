# Miqaat Pass History

Simple single-page tool to:

- Upload bulk ITS numbers for a selected Miqaat and venue.
- Store pass history in browser localStorage.
- View dashboard statistics and recent history.
- Download all records as a CSV file (Excel-compatible).

## Run locally

```bash
python3 -m http.server 8000
```

Then open: <http://localhost:8000>
